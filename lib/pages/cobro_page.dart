import 'package:flutter/material.dart';

class CobroPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Cobro page'),
        ),
      ),
    );
  }
}
